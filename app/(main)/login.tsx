import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SSOComponent = () => {
	// TODO: This page should be the redirected page after SSO login, this means this page should be on a window, we need to 
  return (
	<View>
	  <Text>login</Text>
	</View>
  )
}

export default SSOComponent

const styles = StyleSheet.create({})